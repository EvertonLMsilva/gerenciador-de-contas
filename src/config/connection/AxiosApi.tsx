const AxiosApi = (host: string,
    params?: object,
    header: object,
    body?: object) => {

    console.log('-----', host,
        params,
        header,
        body,)

}

export default AxiosApi;